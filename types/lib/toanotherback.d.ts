export default class Toanotherback{
    static prefix: string;

    static readonly origin: string;

    static host: string;

    static https: boolean;

    static parameters: RequestInit;

    static credentials: boolean;

    static headers: HeadersInit;
}